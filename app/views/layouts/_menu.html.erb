<nav class="navbar navbar-dark bg-dark navbar-expand-lg">  
	<%= link_to root_path, class: "navbar-brand text-info" do %>
		<%= icon('fas fa-camera-retro', size: 48) %> π Contest
	<% end %>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarNav">
		<ul class="navbar-nav ml-auto">
			<% if current_user %>  
				<% if policy(:user).index? %>  
					<li class="nav-item"><%= link_to 'utenti', users_path, class: 'nav-link' %></li>
				<% end %>
				<% if policy(:theme).index? %>  
					<li class="nav-item"><%= link_to 'categorie', themes_path, class: 'nav-link' %></li>
				<% end %>
        <li class="nav-item mx-2">
          <%= link_to current_user.email, edit_user_info_path, class: 'nav-link' %>
        </li>
        <li class="nav-item mx-2">
          <%= link_to icon("fas fa-sign-out-alt fa-fw"), destroy_user_session_path, method: :delete, class: "nav-link" %>
        </li>
			<% else %>
				<li class="nav-item"><%= link_to icon("fas fa-sign-in-alt fa-fw", 'accedi'), new_user_session_path, class: "nav-link" %></li>
			<% end %>
		</ul>
	</div>
</nav>

