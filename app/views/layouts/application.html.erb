<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>PhotoContest</title>

    <meta name="viewport" content="width=device-width, minimum-scale=1.0, initial-scale=1.0, shrink-to-fit=no, user-scalable=yes"/>
    <meta name="description" content="Photo Contest for pi day"/>
    <meta name="keywords" content="Foto, contest, pi greco day"/>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <%= render(partial: 'layouts/menu') unless @no_menu %>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
